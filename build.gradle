// Root build.gradle
plugins {
    id "com.github.jmongard.git-semver-plugin" version "0.4.2"
}

group = 'org.homonoia'
version = semver.version

semver {
    releaseTagNameFormat = "simple-rules-%s"
}